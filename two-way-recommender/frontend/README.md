# Two Way Recommender (Frontend)

This is a Vite + React frontend that connects to your Flask backend.

## Setup
1. Copy `.env.example` to `.env` and set `VITE_API_URL` if your backend is running on another host.
2. Install:
   npm install
3. Run:
   npm run dev

